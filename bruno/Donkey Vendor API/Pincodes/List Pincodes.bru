meta {
  name: List Pincodes
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/vendor/pincodes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
}

docs {
  List all pincodes assigned to vendor along with active/inactive status.
}
