meta {
  name: All Bookings
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/vendor/bookings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
}

docs {
  List all vendor bookings with status, date, search, and pagination query params.
}
